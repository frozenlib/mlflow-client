// use core::panic;
// use std::{
//     fs::{create_dir_all, remove_dir_all},
//     io::{BufRead, BufReader, Stderr, Stdin},
//     path::Path,
//     process::{Child, Command, Stdio},
//     thread::sleep,
// };

// use mlflow_client::client::MlflowClient;

fn main() -> mlflow_client::Result<()> {
    // let _client = MlflowClient::default();

    Ok(())
}
